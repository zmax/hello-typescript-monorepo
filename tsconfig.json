{
  "extends": "./tsconfig.build.json",
  "compilerOptions": {
    "baseUrl": ".",                       /* Base directory to resolve non-absolute module names. */
    "paths": {
      "@beyond/*": ["packages/*/src"]
    }
  }
}
